# Override properties that require environment variables so the context loads in tests
gateway.url=http://localhost:8080
gateway.servicekey=test-service-key
jwt.secret=test-jwt-secret-that-is-long-enough-for-hmac-sha256
cors.allowed-origins=http://localhost:5173
